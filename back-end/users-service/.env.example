# TCP transport (matches gateway's USERS_SERVICE_HOST/PORT)
USERS_SERVICE_HOST=0.0.0.0
USERS_SERVICE_PORT=3001

# PostgreSQL connection (see docker-compose.yml at the repo root)
DATABASE_URL="postgresql://task1:task1@localhost:5432/users_service?schema=public"
