// Shared Prettier config for the whole workspace (front-end + back-end).
// Ported from tensi-backend's .prettierrc.mjs.
export default {
  semi: true,
  singleQuote: true,
  printWidth: 100,
  tabWidth: 2,
  trailingComma: 'all',
  arrowParens: 'always',
};
